/**
 * Dashboard (Phase 8).
 * Khu A: thong ke kho van ban (documents + chunks).
 * Khu B: chat luong he thong (truy_van + lan_chay_eval).
 * Server component, query aggregate truc tiep bang Drizzle, khong tao API route rieng.
 */
export default function TrangDashboard() {
  return (
    <main className="mx-auto max-w-5xl p-8">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="text-sm text-muted-foreground">Sẽ xây ở Phase 8.</p>
    </main>
  );
}
