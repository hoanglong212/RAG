/**
 * CLI nap hang loat: quet data/raw -> extract text -> parse -> chunk -> embed theo lo 50 -> ghi DB.
 * Mot file loi khong lam dung ca me; ghi trang thai vao documents.trang_thai.
 * Co co --reembed de embed lai toan bo chunk khi doi model (Phase 7).
 * Hien thuc o Phase 3.
 */
export {};
